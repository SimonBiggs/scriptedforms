import {} from 'jasmine';

describe('a test', () => {
  it('should do something', () => {
    expect(true).toBe(true)
  })
  it('should do something else', () => {
    expect('string').toBe('string')
  })
})